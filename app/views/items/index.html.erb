<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>トップページ</title>
    <link rel="stylesheet" href="/assets/styles.css">
</head>
<body>
    <header>
        <h1>買い物リスト管理アプリ</h1>
    </header>

    <main>
        <% if user_signed_in? %>
            <h2>ようこそ、<%= current_user.nickname || current_user.email %>さん！</h2>
            <p class="welcome-message">ルームやアイテムリストを確認できます。</p>
            <%= link_to 'ルームへ', rooms_path, class: 'button' %>
            <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'button' %>

            <!-- お問い合わせボタン -->
            <div class="contact-button">
                <%= link_to 'お問い合わせはこちら', new_contact_path, class: 'btn btn-contact' %>
            </div>
        <% else %>
            <h2>ログインまたは新規登録</h2>
            <%= link_to 'ログイン', new_user_session_path, class: 'button' %>
            <%= link_to '新規登録', new_user_registration_path, class: 'button' %>
        <% end %>
    </main>

    <footer>
        <p>&copy; 2024 買い物リスト管理アプリ</p>
    </footer>
</body>
</html>
